---
import bgImage from '../assets/images/better-top-banner/background.jpg';
import itemsImage from '../assets/images/better-top-banner/multiple.png';

interface Props {
  headline: string;
  subheadline: string;
  ctaText: string;
  ctaUrl: string;
  socialProof?: {
    compatibility: string;
    price: string;
    license: string;
  };
}

const { headline, subheadline, ctaText, ctaUrl, socialProof } = Astro.props;
---

<section class="relative overflow-hidden bg-primary-dark">
  <!-- Background image -->
  <div
    class="pointer-events-none absolute inset-0 bg-cover bg-center"
    style={`background-image: url('${bgImage.src}');`}
  ></div>
  <!-- Dark overlay for text readability -->
  <div class="pointer-events-none absolute inset-0 bg-primary-dark/70"></div>

  <div class="relative mx-auto max-w-6xl px-6 py-28 md:py-40 text-center">
    <img src={itemsImage.src} class="object-contain max-w-[350px] mx-auto mb-8"/>
    <h1 class="text-4xl md:text-5xl lg:text-6xl font-bold tracking-tight text-white leading-tight max-w-4xl mx-auto">
      {headline}
    </h1>
    <p class="mt-6 text-lg md:text-xl text-on-dark max-w-3xl mx-auto leading-relaxed">
      {subheadline}
    </p>
    <div class="mt-10 flex flex-col sm:flex-row items-center justify-center gap-4">
      <a
        href={ctaUrl}
        target="_blank"
        rel="noopener"
        class="inline-block rounded-lg bg-primary px-8 py-4 text-lg font-semibold text-primary-dark shadow-2xl hover:bg-primary-light hover:shadow-xl hover:-translate-y-0.5 active:translate-y-0 transition-all duration-300"
      >
        {ctaText}
      </a>
      <a
        href="/#features"
        class="inline-block rounded-lg border-[1.5px] border-white/20 px-8 py-4 text-lg font-semibold text-white hover:border-white/40 hover:bg-white/5 transition-all duration-300"
      >
        See features
      </a>
    </div>
    <p class="mt-6 text-sm text-on-dark-muted">No subscription &bull; No core overrides &bull; Full unobfuscated PHP source</p>

    {socialProof && (
      <div class="mt-16 border-t border-white/10 pt-8">
        <div class="flex flex-wrap justify-center gap-8 md:gap-16 text-center">
          <div>
            <span class="text-2xl font-bold text-white">{socialProof.compatibility}</span>
            <p class="text-sm text-on-dark-muted">Compatibility</p>
          </div>
          <div>
            <span class="text-2xl font-bold text-white">{socialProof.price}</span>
            <p class="text-sm text-on-dark-muted">Price</p>
          </div>
        </div>
      </div>
    )}
  </div>
</section>
